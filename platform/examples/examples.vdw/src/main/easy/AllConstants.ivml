project AllConstants {

    import IIPEcosphere;

    annotate BindingTime bindingTime = BindingTime::compile to .;

    defaultFieldCachingTime = CACHE_NONE;
    
    freeze {
        defaultFieldCachingTime;
    } but (f|f.bindingTime >= BindingTime.runtimeMon);

}