project PlatformConfiguration {

    import TechnicalSetup;
    import AllServices;
    import AllTypes;
    
    annotate BindingTime bindingTime = BindingTime::compile to .
    
    freeze {
        .;
    }
}