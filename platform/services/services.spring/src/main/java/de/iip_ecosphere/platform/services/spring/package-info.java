/**
 * Uses the Spring Local Cloud Deployer as Service Manager Implementation. It is important to keep in mind, that 
 * this package is not an usual Spring Application rather than an integration of plain Java (JSL loading) for optional
 * components and Spring as one potential implementation.
 */
package de.iip_ecosphere.platform.services.spring;
