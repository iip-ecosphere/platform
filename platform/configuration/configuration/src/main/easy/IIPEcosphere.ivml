project IIPEcosphere {

    import Applications; 
    
    annotate BindingTime bindingTime = BindingTime::compile to .;
    
    NonEmptyString iipVer = "0.2.0"; // Maven version of platform
    
    freeze {
        iipVer;
    };
   
}