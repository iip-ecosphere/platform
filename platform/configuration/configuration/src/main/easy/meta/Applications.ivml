project Applications {

    import Devices;
    
    annotate BindingTime bindingTime = BindingTime::compile to .;

    compound Application {
        NonEmptyString id;
        NonEmptyString name;
        String description;
        Version version;
        setOf(ServiceNet) services;
    }
    
    compound ServiceNet {
    }

}