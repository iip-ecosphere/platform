# oktoflow platform: JSON/XML plugin for jackson

JSON implementing plugin through jackson. Can be loaded as plugin or used as JSL component (direct dependency, e.g. for testing).

For convenience and resource consumption reasons, for now this plugin also implements XML through jackson.