package iip.datatypes;

import de.iip_ecosphere.platform.support.ConfiguredName;
import de.iip_ecosphere.platform.support.Ignore;
import de.iip_ecosphere.platform.support.StringUtils;

/**
 * Implementation of the application data type RoutingCommand.
 * Generated by: EASy-Producer.
 */
@ConfiguredName("RoutingCommand")
public class RoutingCommandNewImpl implements RoutingCommandNew {

    @ConfiguredName("cmd")
    private String cmd;

    /**
     * Default constructor. Fields are pre-allocated with default Java values.
     */
    public RoutingCommandNewImpl() {
    }

    /**
     * Copy constructor.
     *
     * @param from the instance to copy the values from
     */
    public RoutingCommandNewImpl(RoutingCommandNew from) {
        this.cmd = from.getCmd();
    }

    @Override
    @Ignore
    public String getCmd() {
        return cmd;
    }

    @Override
    @Ignore
    public void setCmd(String value) {
        this.cmd = value;
    }

    @Override
    public int hashCode() {
        int hc = 0;
        hc += getCmd() != null ? getCmd().hashCode() : 0;
        return hc;
    }

    @Override
    public boolean equals(Object other) {
        boolean eq;
        if (other instanceof RoutingCommandNew) {
            RoutingCommandNew oth = (RoutingCommandNew) other;
            eq = true;
            eq &= getCmd() != null ? getCmd().equals(oth.getCmd()) : true;
        } else {
            eq = false;
        }
        return eq;
    }

    @Override
    public String toString() {
        return StringUtils.toStringShortStyle(this);
    }

}
