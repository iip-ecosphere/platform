# oktoflow platform: Class/bytecode plugin through bytebuddy

Class/bytecode implementing plugin for bytebuddy. Can be loaded as plugin or used as JSL component (direct dependency, e.g. for testing).
