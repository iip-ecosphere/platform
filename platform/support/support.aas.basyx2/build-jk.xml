<?xml version="1.0" encoding="UTF-8"?>
<project name="IIP-Ecosphere support BaSyx AAS extension" default="maven" basedir=".">
	<!-- import of the global SSE Jenkins properties -->
	<property file="${user.home}/global-build.properties"/>
	<property file="${user.home}/qualimaster-build.properties"/>
	<include file="${user.home}/macros.xml"/>
	
    <!-- deploy to SSE Mvn repo -->
	<target name="maven"> 
	    <maven pomFile="support.aas.basyx2.common/pom.xml" goal="deploy"/>
	    <maven pomFile="support.aas.basyx2.server/pom.xml" goal="deploy"/>
	    <maven pomFile="pom.xml" goal="deploy"/>
        <!--<maven pomFile="support.aas.basyx2.plugintests/pom.xml" goal="install"/>-->
		<delete dir="tomcat.8080"/>
		<delete dir="support.aas.basyx2.server/tomcat.8080"/>
	</target>
</project>
