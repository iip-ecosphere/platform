id: serviceMgr
name: Default Service Manager with Test App
version: 0.3
dockerImageName: iip/edgeservicemgr:0.3
dockerImageZipfile: edgeservicemgr-0.3.tar.gz
env:
  - IIP_PORT=${port}
exposedPorts:
  - port: ${port}
    protocol: TCP
networkMode: host
