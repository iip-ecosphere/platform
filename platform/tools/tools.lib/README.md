# oktoflow platform: Basic tools library

Tools and common classes for platform and Maven plugins. As Maven plugins, independent of oktoflow parent POM.

Must be built with Java 8 and not have and other depenencies than the Java library. If we need a similar Maven library, it shall be on the same level, called `tools.lib.maven` and may depend on this library.
