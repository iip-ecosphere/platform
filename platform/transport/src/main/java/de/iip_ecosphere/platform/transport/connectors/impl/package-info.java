/**
 * Platform transport connectors for internal stream-based data transport within the platform. We do not rely on a 
 * single, fixed (potentially slightly better performing) mechanism but leave the decision of the actual mechanism 
 * to the final installation, e.g., due to licensing.
 */
package de.iip_ecosphere.platform.transport.connectors.impl;

